# speech_recognition
To run the code

*optional steps - create a virtual environment*

`git clone`

`python -m venv venv`

`source venv/bin/activate`

`pip install -r requirements.txt`

`python main.py`
